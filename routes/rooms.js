const express = require("express");
const router = express.Router();
const { User } = require("../schemas/user.js");
const { Sitter } = require("../schemas/sitter.js");
const { Room } = require("../schemas/user.js");
const { Chat } = require("../schemas/user.js");